# Bench results (JSON par run) — générés au run réel, pas committés.
bench/results/*.json
# Secrets & inventaire réel
ansible/inventory.ini
*.env
# Go
exporter/cost-exporter
# Python
__pycache__/
*.pyc
# État OMC (session Claude Code)
.omc/
